$-- Global Stratum Article Template — Policy Paper Style
$-- Usage: pandoc input.docx --template=globalstratum.tex --pdf-engine=pdflatex -V category="..." -V lede="..." -o output.pdf

\documentclass[12pt,a4paper]{article}

%% ── PACKAGES ──────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[dvipsnames,svgnames]{xcolor}
\usepackage[top=1.15in,bottom=1.2in,left=1.25in,right=1.25in,headheight=15pt]{geometry}
\usepackage{microtype}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{mdframed}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage[hidelinks]{hyperref}
\usepackage{helvet}

%% Pandoc compatibility
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%% ── COLORS ────────────────────────────────────────────────────────────────
\definecolor{gsAccent}{HTML}{4DB6AC}
\definecolor{gsDark}{HTML}{1a1a2e}
\definecolor{gsMuted}{HTML}{888888}
\definecolor{gsBox}{HTML}{F5F8F8}

%% ── HYPERREF ──────────────────────────────────────────────────────────────
\hypersetup{colorlinks=true,linkcolor=gsAccent,urlcolor=gsAccent,citecolor=gsMuted}

%% ── HEADER / FOOTER ───────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\fontsize{9}{11}\selectfont\color{gsMuted}\thepage}

%% First page has no header/footer
\fancypagestyle{firstpage}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\fontsize{9}{11}\selectfont\color{gsMuted}\thepage}
}

%% ── SECTION HEADINGS ──────────────────────────────────────────────────────
\setcounter{secnumdepth}{1}

\titleformat{\section}[block]
  {\normalfont\large\bfseries\sffamily\color{gsDark}}
  {\textcolor{gsAccent}{\thesection.\enspace}}{0em}
  {\MakeUppercase}
  [\vspace{4pt}\textcolor{gsAccent}{\rule{\linewidth}{1pt}}]

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\sffamily}
  {}{0em}{\MakeUppercase}

\titlespacing*{\section}{0pt}{2em}{0.85em}
\titlespacing*{\subsection}{0pt}{1.5em}{0.5em}

%% ── LISTS ─────────────────────────────────────────────────────────────────
\setlist[itemize]{leftmargin=1.75em,itemsep=0.25em,topsep=0.5em}
\setlist[enumerate]{leftmargin=1.75em,itemsep=0.25em,topsep=0.5em}
\setlist[itemize,1]{label=\textcolor{gsAccent}{\textbullet}}

%% ── BLOCKQUOTE ────────────────────────────────────────────────────────────
\newmdenv[
  topline=false,bottomline=false,rightline=false,
  linewidth=3pt,linecolor=gsAccent,
  skipabove=1.25em,skipbelow=1.25em,
  innerleftmargin=1em,innerrightmargin=0.5em,
  innertopmargin=0.4em,innerbottommargin=0.4em,
  backgroundcolor=gsBox,
]{pullquote}
\renewenvironment{quote}{\begin{pullquote}\itshape}{\end{pullquote}}

%% ── EXECUTIVE SUMMARY BOX ─────────────────────────────────────────────────
\newmdenv[
  topline=true,bottomline=true,rightline=true,leftline=true,
  linewidth=0.6pt,linecolor=black!20,
  skipabove=1.5em,skipbelow=2em,
  innerleftmargin=1.25em,innerrightmargin=1.25em,
  innertopmargin=1em,innerbottommargin=1em,
  backgroundcolor=gsBox,
]{summarybox}

%% ── SPACING ───────────────────────────────────────────────────────────────
\setlength{\parskip}{0.65em}
\setlength{\parindent}{0pt}
\setkeys{Gin}{width=\linewidth,keepaspectratio}

%% ═════════════════════════════════════════════════════════════════════════════
\begin{document}
\thispagestyle{firstpage}

%% TOP LINE — date and publication
\noindent
{\fontsize{7.5}{10}\selectfont\sffamily\color{gsMuted}%
\MakeUppercase{Global Stratum}%
$if(date)$\enspace\textperiodcentered\enspace\MakeUppercase{$date$}$endif$%
$if(category)$\enspace\textperiodcentered\enspace\MakeUppercase{$category$}$endif$}

\vspace{1.1em}

%% TITLE
{\fontsize{34}{38}\selectfont\bfseries\sffamily\color{gsDark}%
\MakeUppercase{$title$}\par}

\vspace{0.6em}

%% SUBTITLE (lede used as italic subtitle under title)
$if(lede)$
{\fontsize{13}{17}\selectfont\itshape\color{black!75} $lede$\par}
$endif$

\vspace{0.75em}
\noindent\rule{\linewidth}{0.5pt}
\vspace{1.25em}

%% EXECUTIVE SUMMARY BOX
$if(lede)$
\begin{summarybox}
{\fontsize{8.5}{11}\selectfont\sffamily\bfseries\MakeUppercase{Executive Summary}}

\vspace{0.4em}
{\fontsize{10.5}{15}\selectfont\itshape $lede$\par}
\end{summarybox}
$endif$

%% BODY
\begin{spacing}{1.2}
$body$
\end{spacing}

%% FOOTER
\vspace{2em}
\noindent\rule{\linewidth}{0.4pt}
\vspace{0.3em}

{\fontsize{7}{9}\selectfont\sffamily\color{gsMuted}%
\textcopyright\enspace Global Stratum.\enspace
All analysis represents the views of the author.\enspace
newsletter.cfod.co.uk}

\end{document}
